---
interface Props {
  label?: string;
  variant?: 'light' | 'dark' | 'muted';
}

const { label, variant = 'light' } = Astro.props;

const variantClasses: Record<string, string> = {
  light: 'bg-white border-neutral-200',
  dark: 'bg-neutral-900 border-neutral-700',
  muted: 'bg-neutral-100 border-neutral-200',
};
---

<div>
  {label && (
    <span class="mb-2 block font-heading text-xs font-semibold uppercase tracking-wider text-neutral-500">
      {label}
    </span>
  )}
  <div
    class:list={[
      'border p-6',
      variantClasses[variant],
    ]}
  >
    <slot />
  </div>
</div>
