---
interface Props {
  id: string;
  title: string;
  subtitle?: string;
  variant?: 'light' | 'dark';
}

const { id, title, subtitle, variant = 'light' } = Astro.props;

const isDark = variant === 'dark';
---

<section
  id={id}
  class:list={[
    'border-t py-16 px-0',
    isDark
      ? 'bg-neutral-900 text-neutral-100 border-neutral-700'
      : 'bg-white text-neutral-700 border-neutral-200',
  ]}
>
  <h2 class:list={[isDark && 'text-neutral-100']}>{title}</h2>
  {subtitle && (
    <p class:list={['mt-2 text-base', isDark ? 'text-neutral-400' : 'text-neutral-500']}>
      {subtitle}
    </p>
  )}
  <div class="mt-10">
    <slot />
  </div>
</section>
